const infiniteProcessor = extend(LogicBlock, "infinite-processor", {});
const armoredSwitch = extend(SwitchBlock, "armored-switch", {});
const infiniteMessage = extend(MessageBlock, "infinite-message", {});
const infiniteMemoryCell = extend(MemoryBlock, "infinite-memory-cell", {});

const sorterDisplay = extend(Sorter, "sorter-display", {});
const smallFramelessDisplay = extend(LogicDisplay, "small-frameless-display", {});
const framelessDisplay = extend(LogicDisplay, "frameless-display", {});
const largeFramelessDisplay = extend(LogicDisplay, "large-frameless-display", {});

const boltPlayer = extend(PowerTurret, "bolt-player", {});
const flamePlayer = extend(PowerTurret, "flame-player", {});
const sparkPlayer = extend(PowerTurret, "spark-player", {});

const godlyProcessor = extend(LogicBlock, "godly-processor", {});